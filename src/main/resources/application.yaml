spring:
  data:
    mongodb:
      host: '${MONGO_HOST:localhost}'
      port: 27018
      database: '${MONGO_DB:moviemanagement}'

  cloud:
    kubernetes:
      discovery:
        register: true

      config:
        enabled: true
        sources:
          - namespace: default
            name: ctferro-db-config
      reload:
        enabled: true
        mode: polling
        period: 1000